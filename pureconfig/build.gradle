archivesBaseName = "rabbitmq-client-pureconfig_$scalaVersion"

dependencies {
    api project(":core")

    api "com.typesafe:config:$typesafeConfigVersion"
    api "com.github.pureconfig:pureconfig_${scalaVersion}:$pureconfigVersion"
}
