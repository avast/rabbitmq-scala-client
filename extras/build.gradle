archivesBaseName = "rabbitmq-client-extras_$scalaVersion"

dockerCompose.isRequiredBy(test)

test.doFirst {
    dockerCompose.exposeAsSystemProperties(test)
}

dependencies {
    api project(":core")

    testImplementation project(":pureconfig")
}
