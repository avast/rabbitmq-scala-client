archivesBaseName = 'rabbitmq-client-extras-circe'

dependencies {
    compile project(":core")
    compile project(":extras")

    compile "io.circe:circe-core_$scalaVersion:$circeVersion"
    compile "io.circe:circe-parser_$scalaVersion:$circeVersion"
}
